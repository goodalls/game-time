global.Image = class {};
global.window = {};

const chai = require('chai');
const assert = chai.assert;

const Player = require ('../lib/Player.js');
const Projectile = require ('../lib/Projectile.js');
const Keyboard = require ('../lib/Keyboard.js');
const Game = require ('../lib/Game.js');

describe('Index unit testing', () => {

  it('should start round zero, one ect if statments', () => {
    
  });

  it('should round zero stuff, set loadScreen to false, set intiated to true, spawn.', () => {
    
  });

  it('should round one stuff', () => {
    
  });

  it('should round two stuff', () => {
    
  });

  it('should round three stuff', () => {
    
  });

  it('should enemyHandler, set enemy target X / Y,  detect collision, cause game over ', () => {
    
  });

  it('should collision handler, dtetct collsion, splice enemy and bullet, add to score', () => {
    
  });

  it('should instanciate keyboard', () => {
    
  });

  it('should keys arrow keys', () => {
    
  });

  it('should', () => {
    
  });

});